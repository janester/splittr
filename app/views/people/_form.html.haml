-unless @people.nil?
  %ul
    -@people.each do |person|
      %li= person.name.titleize


= form_for Person.new, :remote => true do |f|
  =f.label :name
  =f.text_field :name, :autofocus => "true"
  =f.submit "Add Another Person"